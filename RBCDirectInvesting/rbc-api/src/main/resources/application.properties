server.port=8080
spring.application.name=rbc-direct-investing-api

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/rbc_investing
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# JWT
jwt.secret=${JWT_SECRET:rbc-direct-investing-demo-secret-key-min-256-bits-long!!}
jwt.expiration-ms=86400000

# Finnhub
finnhub.api.key=${FINNHUB_API_KEY:}
finnhub.api.base-url=https://finnhub.io/api/v1

# OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
